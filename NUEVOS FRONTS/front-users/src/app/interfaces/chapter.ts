export interface Chapter {
  storyId: number;
  storyTitle: string;
  chapterId: number;
  chapterTitle: string;
  chapterContent: string;
  dayNumber: number;
  unlockCondition: string;
  grantedBy: number;
  grantDate: string;
}